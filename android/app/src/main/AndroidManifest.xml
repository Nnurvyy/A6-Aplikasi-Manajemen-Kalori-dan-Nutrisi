<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ── Izin Health Connect (READ only) ───────────────────────────────── -->
    <uses-permission android:name="android.permission.health.READ_STEPS"/>
    <uses-permission android:name="android.permission.health.READ_HEART_RATE"/>
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED"/>
    <uses-permission android:name="android.permission.health.READ_OXYGEN_SATURATION"/>

    <!-- Izin notifikasi (wajib Android 13+ / API 33) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- SCHEDULE_EXACT_ALARM: untuk Android 12 (API 31-32) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <!-- USE_EXACT_ALARM: untuk Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <!-- Wajib agar notifikasi bisa dijadwal ulang setelah device restart -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:label="nutritrack_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <!-- Intent launcher utama app -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Intent filter untuk dialog penjelasan izin Health Connect -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>

        </activity>

        <!-- Activity untuk handle alur request permission Health Connect -->
        <activity
            android:name="androidx.health.connect.client.PermissionController$HealthDataRequestPermissionsActivity"
            android:exported="true"/>

        <!-- URL Privacy Policy — wajib untuk Health Connect -->
        <meta-data
            android:name="health_connect_privacy_policy_url"
            android:value="https://sites.google.com/view/nutritrack-privacy-policy/halaman-muka"/>

        <!-- Receiver untuk reschedule notifikasi setelah device restart -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Receiver untuk memicu notifikasi terjadwal -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

        <!-- ML Kit untuk barcode scanning -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode"/>

    </application>

    <!-- Queries: WAJIB di luar <application> -->
    <queries>
        <!-- Package Health Connect -->
        <package android:name="com.google.android.apps.healthdata"/>

        <!-- Intent Process Text (bawaan Flutter) -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>